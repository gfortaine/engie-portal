import { createFileRoute } from '@tanstack/react-router';
import { {{pascalCase name}}Page } from './-{{pascalCase name}}Page';

export const Route = createFileRoute('/{{dashCase name}}')({
  component: {{pascalCase name}}Page,
});
